<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flab.foodeats.shop.mapper.ShopMapper">

    <!-- 가맹점 등록 (기본)  -->
    <insert id="registerEssentialInfo" parameterType="EssentialShopInfo">
        INSERT
        INTO shop_essential_info
        VALUES(#{shopId}, #{essentialShopInfo.category}, #{essentialShopInfo.brand}, #{essentialShopInfo.location},
        #{essentialShopInfo.phone});
    </insert>
    <!-- 가맹점 수정 (기본) -->
    <update id="updateEssentialInfo" parameterType="EssentialShopInfo">
        UPDATE shop_essential_info
        SET category = #{essentialShopInfo.category}, brand = #{essentialShopInfo.brand}, location = #{essentialShopInfo.location},
        phone = #{essentialShopInfo.phone}
        WHERE shopId = #{shopId};
    </update>
    <!-- 가맹점 삭제 (기본) -->
    <delete id="deleteEssentialInfo" parameterType="EssentialShopInfo">
        DELETE Shop_info, shop_convenience_info, shop_status_info
        FROM shop_essential_info
        INNER JOIN shop_convenience_info ON shop_essential_info.shopId = shop_convenience_info.shopId
        INNER JOIN shop_status_info ON shop_essential_info.shopId = shop_status_info.shopId
        WHERE shop_essential_info.shopId = #{shopId};
    </delete>


    <!-- 가맹점 등록 (상태)  -->
    <insert id="registerStatusInfo" parameterType="StatusShopInfo">
        INSERT
        INTO shop_status_info(shopId,openTime,closeTime,closingDay)
        VALUES(#{shopId}, #{statusShopInfo.openTime}, #{statusShopInfo.closeTime},
        #{statusShopInfo.closingDay});
    </insert>
    <!-- 가맹점 수정 (상태) -->
    <update id="updateStatusInfo" parameterType="StatusShopInfo">
        UPDATE shop_status_info
        SET openTime = #{statusShopInfo.openTime}, openTime = #{statusShopInfo.closeTime}, closingDay = #{statusShopInfo.closingDay}
        WHERE shopId = #{shopId};
    </update>


    <!-- 가맹점 등록 (편리)  -->
    <insert id="registerConvenienceInfo" parameterType="ConvenientShopInfo">
        INSERT
        INTO shop_convenience_info
        VALUES(#{shopId}, #{convenientShopInfo.grade}, #{convenientShopInfo.deliveryMethod}, #{convenientShopInfo.shopPaymentMethod});
    </insert>
    <!-- 가맹점 수정 (편리) -->
    <update id="updateConvenienceInfo" parameterType="ConvenientShopInfo">
        UPDATE shop_convenience_info
        SET grade = #{convenientShopInfo.grade}, deliveryMethod = #{convenientShopInfo.deliveryMethod}, shopPaymentMethod = #{convenientShopInfo.shopPaymentMethod}
        WHERE shopId = #{shopId};
    </update>


    <!-- 가맹점 기본정보 조회 By ShopId -->
    <select id="findEssentialInfoById" parameterType="Integer" resultType="Integer">
        SELECT *
        FROM shop_essential_info
        WHERE shopId = #{shopId};
    </select>
    <!-- 가맹점 상태정보 조회 By ShopId -->
    <select id="findStatusInfoById" parameterType="Integer" resultType="Integer">
        SELECT *
        FROM shop_status_info
        WHERE shopId = #{shopId};
    </select>
    <!-- 가맹점 편리정보 조회 By ShopId -->
    <select id="findConvenienceInfoById" parameterType="Integer" resultType="Integer">
        SELECT *
        FROM shop_convenience_info
        WHERE shopId = #{shopId};
    </select>

    <!-- 회원 전체 조회 -->
    <select id="ShopListAllInfo" resultType="EssentialShopInfo">
        SELECT *
        FROM Shop_info;
    </select>
</mapper>
